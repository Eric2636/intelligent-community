# 智能社区小程序项目状态总结

最后更新：2026-07-28

## 当前状态

项目核心业务和第一、第二阶段优化已有本地实现，但当前状态仍是分阶段开发和环境待验，不能表述为全面完成或已经可以发布：

- 第一阶段：历史用户名/头像同步、默认头像、统一授权弹框、小区市场游客查询、“我的任务”返回修复和跑腿下线已实现。
- 第二阶段：小程序 RxJS 热搜索、10 个列表 1/3 行规则、更多服务精简、纯文本反馈及后台 Enter/latest-wins 已实现。
- 第三阶段：论坛、任务、市场和系统通知中心已有本地实现。
- 第四阶段：全接口日志和接口管理已有本地实现，包括后端数据模型、迁移、接口注册、中间件、管理 API、后台页面、OpenAPI 和清理脚本；本地库建表与成功请求写入已验证，但清理演练、目标环境迁移及完整验收尚未完成。
- 2026-07-28 已备份并迁移本机 `127.0.0.1:3308/ic_test`；测试/生产数据库迁移、微信开发者工具 npm 构建/包体检查及 320/375/430 三档视觉验收尚未执行。

详细的 36 项状态统计见 `FEATURE_STATUS.md`。

## 已实现的核心模块

### 业主互助

- 任务列表、详情、发布、草稿、撤回、领取、完成、确认和取消。
- 我的发布/领取及状态筛选。
- 任务作者头像与统一默认头像。
- 500ms 热搜索和异步 latest-wins。

### 小区留言

- 公告、帖子列表/详情、发帖、回复、点赞、收藏和我的内容。
- 作者用户名与头像展示及历史资料同步。
- 500ms 热搜索和列表文字限制。

### 小区市场

- 游客公开查询和登录后的个性化收藏/点赞状态。
- 商品发布、评论、收藏、购买及订单状态流程。
- 我的商品、收藏和订单。
- 500ms 热搜索、分类、排序和下架商品权限控制。

### 用户与个人中心

- 微信手机号授权、本地登录态、身份选择和资料编辑。
- 受保护操作使用当前页授权弹框，授权后需再次点击。
- “我的”页业务入口按任务、留言、市场分区展示。
- “更多服务”仅保留消息通知和意见反馈；个人资料通过头像区域进入，退出登录位于页面底部。

### 意见反馈

- 登录后提交纯文本 `content`。
- trim 后限制 1–500 个 Unicode code points。
- 不包含反馈类型、联系方式或图片。

### 消息通知

- 已接入论坛帖子/评论回复、任务状态、市场订单和超级管理员系统通知。
- 通知创建与对应业务状态写入保持事务一致，并通过业务版本、回复编号或发布意图去重；操作人不会收到自己的业务通知。
- 小程序支持服务端未读数、全部已读、详情跳转、目标不存在提示和仿 iOS/微信列表左滑软删除。
- 明确不包含点赞和收藏通知。详细事件矩阵见 `docs/消息通知事件清单.md`。

## 实现中与尚未完成的功能

### 第四阶段：接口日志与接口管理

后台现有“小程序接口错误日志”旧页面不等于第四阶段已经实现。当前 `dev` 工作区已有：

- `ApiEndpoint`、`ApiAccessLog`、`ApiErrorLog` 模型及 `20260726160000_add_api_access_logging` 迁移；
- 从 Koa Router 自动同步标准路由、新接口默认开启的注册服务；
- 最外层访问日志中间件、基础路径/异常脱敏、日志写入失败隔离和 5xx 独立错误日志基础。
- 仅超级管理员可用的接口管理、访问日志/错误日志查询、筛选与 CSV 导出 API；
- 后台接口管理、访问日志、错误日志页面及菜单/路由权限；
- 描述/开关变更分别写入管理员操作日志的行为与测试、OpenAPI 声明和 90 天分批清理脚本。
- 保留周期行为测试，以及只有可信 socket peer 才读取转发头的代理 IP 白名单限制。

以下能力仍未完成或未验收：

- 在专用测试数据库执行迁移和 89/90/91 天清理边界演练；
- 将清理脚本接入目标环境的低峰期定时任务并核对运维输出；
- 目标环境代理白名单配置、脱敏、日志写入故障、500 兜底、性能及全量自动测试验收。

## 页面结构与统计口径

页面数从 `app.json` 自动核实，不按目录名目测：

```bash
node -e 'const a=require("./app.json");const main=a.pages.length;const sub=a.subpackages.map(x=>({root:x.root,count:x.pages.length}));const allSub=sub.reduce((n,x)=>n+x.count,0);const businessPublic=sub.filter(x=>x.root.startsWith("package")).reduce((n,x)=>n+x.count,0);console.log({main,sub,allSub,businessPublic,total:main+allSub});'
```

当前统计：

| 口径 | 数量 | 说明 |
| --- | ---: | --- |
| 主包页面 | 5 | 4 个 Tab 页 + 1 个兼容登录页 |
| 全部 subpackage 页面 | 20 | 6 个 subpackage 声明的页面总和 |
| 业务/公共分包页面 | 18 | Task 3 + Forum 4 + Mall 6 + Common 5 |
| 个人资料/设置分包页面 | 2 | 个人资料编辑 1 + 设置 1 |
| 全部注册页面 | 25 | 主包 5 + 全部分包 20 |

`packageCommon` 的 5 页包含消息通知、意见反馈、关于、用户协议和隐私政策。页面存在不代表它一定出现在“更多服务”菜单。

## 技术与质量状态

- 微信小程序原生框架、JavaScript/WXML/LESS、TDesign Miniprogram、RxJS。
- Koa + TypeScript + Prisma API，Vue 3 + Ant Design Vue 后台。
- 第三阶段 fresh 验收命令及结果见 `docs/superpowers/baselines/2026-07-26-phase-3-acceptance.md`。
- 小程序全仓 ESLint 仍按 4544/0 历史基线进行指纹比较；只能在新增指纹为 0 时通过，不能描述为全仓 lint 退出码 0。
- 当前改动未提交、未推送、未合并、未部署。

## 下一步边界

1. 在有权限的微信开发者工具中构建 npm，验证 RxJS 解析和包体。
2. 完成 320px、375px、430px 三档视觉验收。
3. 在已备份的专用开发/测试数据库执行并验证当前五份迁移。
4. 完成第四阶段测试环境迁移、清理演练、定时任务接入和完整验收。
5. 经明确授权后再进入提交、测试分支合并和体验版流程。
