# 智能社区小程序快速启动指南

最后更新：2026-07-28

## 快速开始

1. 在项目根目录安装依赖：

   ```bash
   npm install
   ```

2. 用微信开发者工具导入当前项目根目录。
3. 在微信开发者工具执行“工具 → 构建 npm”，确认 `rxjs` 和 TDesign 依赖可解析。
4. 根据开发/体验版本目标确认 API 环境后预览。

不要为自动化操作擅自开启微信开发者工具“安全设置”。当前开发者工具服务端口尚未获授权，构建 npm、包体和模拟器视觉检查仍需有权限的人员手动完成。

## 主要入口

底部 TabBar 当前有 4 项：

1. 业主互助
2. 小区留言
3. 小区市场
4. 我的

游客可以浏览公开内容。发布、领取、回复、收藏、下单和私有列表等操作会在当前页弹出授权登录框；授权成功后不会自动执行原操作，需要再点击一次。

## 个人中心

“我的”页当前分为：

- 业主互助：我的任务、我的草稿、我的撤回。
- 小区留言：我的帖子、收藏的帖子。
- 小区市场：我发布的、我的订单、收藏的商品。
- 更多服务：仅消息通知、意见反馈。

登录后点击头像区域进入个人资料编辑，这是独立入口，不属于“更多服务”。退出登录按钮位于页面底部。关于、用户协议、隐私政策和设置页面用于独立流程或兼容入口，不在当前“更多服务”菜单中。

## 快速体验

### 业主互助

1. 浏览任务列表并尝试输入关键词，停止输入 500ms 后自动查询。
2. 点击发布；游客先完成授权，再次点击后进入发布页。
3. 用不同账号验证领取、提交完成和发布者确认。
4. 在“我的任务”切换我发布的/我领取的，多次切换后一次返回应回到“我的”。

### 小区留言

1. 浏览公告和帖子，验证热搜索、清空恢复默认列表。
2. 登录后发帖、回复、点赞和收藏。
3. 修改用户名或头像，检查历史帖子和回复展示同步。

### 小区市场

1. 清空登录态，验证分类、在线商品列表、详情和评论可查询。
2. 登录后验证收藏/点赞个性化状态，以及发布、评论、购买和订单。
3. 确认游客不能查看他人的下架商品，发布者可查看自己的下架商品。

### 消息通知

1. 使用两个账号分别验证论坛帖子回复、评论回复，以及任务领取/放弃/提交/确认/驳回/取消和市场下单/取消/完成通知。
2. 检查“我的”和底部导航未读数；点击未读消息后数量应以服务端结果更新，“全部已读”后归零。
3. 点击论坛、任务和市场通知应进入对应详情；删除目标后点击应提示“内容已不存在”。
4. 在真机左滑通知项，应显示红色“删除”；删除后当前账号不可见，另一账号数据不受影响。
5. 系统通知直接在列表阅读；点赞和收藏不应生成通知。

### 意见反馈

1. 从“更多服务”进入意见反馈。
2. 页面应只有内容输入框和提交按钮。
3. 游客首次提交只完成授权；授权后再次点击才提交。
4. 内容 trim 后必须为 1–500 个 Unicode code points；不提供类型、联系方式或图片。

## 项目结构

```text
intelligent-community/
├── pages/                     # 主包 5 页：4 个 Tab + 兼容登录页
├── pages/my/info-edit/        # 独立 subpackage：个人资料编辑 1 页
├── pages/setting/             # 独立 subpackage：设置 1 页
├── packageTask/               # 任务分包 3 页
├── packageForum/              # 论坛分包 4 页
├── packageMall/               # 市场分包 6 页
├── packageCommon/             # 公共分包 5 页
├── components/                # 公共组件
├── custom-tab-bar/            # 自定义 TabBar
├── api/                       # API 调用封装
├── utils/                     # 通用工具
└── docs/                      # 正式说明、设计、计划与验收记录
```

以 `app.json` 自动统计：主包 5 页，全部 subpackage 20 页；其中四个 `package*` 业务/公共分包含 18 页，个人资料与设置另有 2 页，共注册 25 页。

## 本地验证

当前小程序本地配置使用 `config.env.js = local`，API 为 `http://127.0.0.1:3000`。列表缓存按 API 地址隔离；HTTP 4xx/5xx 会显示错误，只有断网或超时时才回退同环境缓存并提示“当前展示缓存数据”。

```bash
node --test test/*.test.js
node --import tsx --test test/*.spec.ts
node test/production-regressions.js
```

小程序全仓 lint 有已记录的历史基线，不能只凭 `npm run lint` 的非零退出码判断本次新增问题。完整的 4544 基线指纹比较命令见 `docs/superpowers/baselines/2026-07-26-phase-3-acceptance.md`。

## 发布边界

- 只在 `dev` 修改代码和文档。
- 测试环境只从 `test` 分支发布，生产环境只从生产分支发布。
- 本地验证不代表已经提交、推送、合并或部署。
- 2026-07-28 已备份并迁移本机 `127.0.0.1:3308/ic_test`；测试和生产数据库仍须分别确认目标、备份和回滚方案后迁移。
- 第四阶段接口日志已有本地实现、数据库建表和成功请求写入验证、保留周期及双字段 PATCH 操作日志行为测试；清理演练、定时任务接入、目标环境迁移/代理配置和故障/性能/脱敏验收尚未完成。字段与待办见 `../intelligent-community-admin/docs/接口访问日志说明.md`。
- 完整体验版检查按 `docs/发布前自测清单.md` 执行。
