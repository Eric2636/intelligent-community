# 智能社区小程序功能说明

最后更新：2026-08-08

## 项目概述

智能社区小程序基于微信小程序原生框架和 TDesign Miniprogram 开发，当前主要提供业主互助、小区留言、小区市场和个人中心能力。业务数据通过同级 `intelligent-community-admin` API 服务持久化；Mock 仅用于明确启用的本地调试场景，不是当前正式数据来源。

## 用户与授权

- 游客可以浏览公开内容，小区市场公开分类、在线商品列表、详情和评论不强制登录。
- 发布、领取、回复、收藏、下单、私有列表和提交反馈等受保护操作，会在当前页弹出授权登录/取消弹框。
- 授权完成后不会自动重放原操作，用户需要再次点击。
- 登录用户点击“我的”页头像区域进入个人资料编辑；退出登录按钮位于“我的”页底部，不属于“更多服务”。
- 用户名或头像修改后，任务、帖子/回复、商品/评论和订单的历史展示快照会同步更新；空头像统一使用 TDesign `user` 矢量图。
- 用户标签由当前用户身份实时统一计算：`User.identityType` 保存默认身份（业主/小区外人员），`AdminUser.boundUserId` 保存唯一的管理员绑定；若绑定了启用中的管理员，则显示该管理员身份（第三方显示机构名、超级管理员显示“平台管理员”）。标签不区分发布入口，也不从内容历史快照读取。
- 用户标签仅在需要作者身份识别的模块展示；小区市场列表和详情不展示发布者用户标签。
- 管理员解绑、禁用或身份变更后，全部历史内容下次查询即同步显示最新标签；无默认身份时不显示标签，后续需身份的操作仍按原流程选择身份。
- 所有启用中的后台管理员都可直接发布公告；小区留言、业主互助任务和小区市场商品必须使用该管理员绑定且未冻结的小程序用户发布。后台操作管理员仍单独保留用于权限控制和操作审计，发布入口不影响用户标签。
- 被冻结用户不能重新登录；已有登录态访问受保护接口时会被清除本地会话并提示联系管理员。公开内容列表仍可浏览。

## 功能模块

### 1. 业主互助

- 任务列表、详情、分页、下拉刷新和状态筛选。
- 发布、草稿、撤回、领取、提交完成、发布者确认和取消。
- “我的任务”支持我发布的、我领取的、草稿和撤回视图；Tab 切换不增加页面历史。
- 列表支持 RxJS 500ms 热搜索，搜索词会 trim，空白恢复默认列表，重复关键词不重复查询，旧请求不能覆盖最新结果。

### 2. 小区留言

- 公告、帖子列表、置顶、详情、分页和下拉刷新。
- 发帖、回复、点赞、收藏、我的帖子和我的收藏。
- 帖子和回复展示作者用户名与头像。
- 列表支持与任务中心一致的 RxJS 热搜索规则。

### 3. 小区市场

- 游客可查询分类、在线商品列表、详情和评论；有效登录态会补充收藏和点赞状态。
- 发布商品、图片/视频、评论、点赞、收藏、购买和订单状态操作。
- 我的商品、我的收藏、我的订单及订单详情。
- 列表支持 RxJS 500ms 热搜索、分类和排序；下架商品仅发布者登录后可见。
- 发布者可在本人信息详情或“我发布的”中点击“更多”，进行编辑、公开、隐藏或删除；删除前会二次确认，其他用户不显示管理入口。
- 公共列表移除装饰性右箭头，整张卡片均可点击并提供按压反馈。

### 列表返回规则

- 从任务、留言或市场详情返回列表时，不重新加载整页数据，因此筛选条件、已加载分页和滚动位置保持不变。
- 详情中的状态、收藏、编辑、隐藏或删除结果通过页面事件同步到对应列表项；用户主动下拉刷新时才重新请求整页数据。

### 4. 个人中心

“我的”页按业务分区展示入口：

- 业主互助：我的任务、我的草稿、我的撤回。
- 小区留言：我的帖子、收藏的帖子。
- 小区市场：我发布的、我的订单、收藏的商品。
- 更多服务：仅消息通知、意见反馈。

个人资料编辑通过登录后的头像区域进入，是独立入口，不放在“更多服务”。关于、协议、隐私和设置页面仍在代码包中，用于对应的独立流程或兼容入口，也不属于当前“更多服务”菜单。

### 5. 消息通知

- 论坛帖子被回复、评论被回复时通知对应作者。
- 任务被领取、放弃、提交完成、确认完成、驳回完成或由发布者取消时通知另一方。
- 市场下单、取消订单和卖家完成订单时通知交易另一方。
- 超级管理员可向全部已启用用户发布系统通知；点赞和收藏不产生通知。
- 通知列表按时间倒序分页，区分已读/未读，支持全部已读；“我的”和自定义底栏使用服务端未读数。
- 点击业务通知先标记已读，再校验并进入帖子、任务或订单详情；目标已删除或无权访问时提示“内容已不存在”。系统通知在列表内直接阅读。
- 左滑显示红色“删除”操作，删除仅对当前用户软删除，不物理移除记录。
- 登录保护遵循统一规则：授权成功后不自动加载私有列表，需要用户再次点击“加载内容”。

完整事件、接收人、去重键和跳转规则见 `docs/消息通知事件清单.md`。

### 6. 意见反馈

- 页面只包含一个 TDesign 多行内容输入框和提交按钮。
- 提交要求登录；授权完成后不自动提交，需要再次点击。
- 前后端都会 trim 内容，并限制为 1–500 个 Unicode code points。
- 请求体只包含 `content`；不包含反馈类型、联系方式或图片。

### 7. 后台与接口日志

后台现有用户、内容、分类和旧日志页面继续保留；后台列表搜索和登录支持普通 Enter，但不改为热搜索。

第四阶段接口日志处于实现中：

- 后端已有接口注册表、普通访问日志表、4xx/5xx 错误日志表及迁移，路由会按“请求方式 + 标准路径”注册，新接口默认开启普通日志。
- 已有最外层日志中间件和基础脱敏；关闭普通日志时，4xx/5xx 仍走独立错误日志，并保存脱敏、限长的路径参数/查询参数/JSON 请求体快照；日志持久化失败不得改变业务响应，CSV 不导出请求快照。
- 超级管理员查询/编辑接口、完整筛选与 CSV 导出、后台接口管理/访问日志/错误日志页面、OpenAPI 和 90 天分批清理脚本已有实现。
- 保留周期服务已有 90 天边界与 5,000 行批次行为测试；可信代理只有在 `TRUST_PROXY=true` 且 socket peer 命中 `TRUST_PROXY_IPS` 时才读取转发头。
- 同一 PATCH 同时修改接口描述和日志开关时，会分别写入两类管理员操作日志，且已有行为测试。数据库迁移/清理演练、部署定时任务、目标环境代理配置、故障隔离、脱敏、性能和全链路环境验收尚未完成。

字段、安全边界和当前待办见同级后端仓库 `docs/接口访问日志说明.md`。在上述待办及完整测试完成前，不得把第四阶段标记为已验收。

## 列表展示规则

任务、论坛、市场及“我的”相关共 10 个业务列表统一使用：

- 标题最多 1 行；
- 正文或摘要最多 3 行；
- 超出内容以省略号隐藏；
- Flex 文本区域限制宽度，避免头像、金额、状态和时间挤出屏幕。

## 页面路由统计

统计以 `app.json` 为唯一依据，可运行：

```bash
node -e 'const a=require("./app.json");const main=a.pages.length;const sub=a.subpackages.map(x=>({root:x.root,count:x.pages.length}));const allSub=sub.reduce((n,x)=>n+x.count,0);const businessPublic=sub.filter(x=>x.root.startsWith("package")).reduce((n,x)=>n+x.count,0);console.log({main,sub,allSub,businessPublic,total:main+allSub});'
```

当前结果：

- 主包注册 5 页：任务、论坛、市场、我的 4 个 Tab 页，以及 1 个兼容登录页。
- 6 个 subpackage 声明合计 20 页。
- 其中 `packageTask` 3 页、`packageForum` 4 页、`packageMall` 6 页、`packageCommon` 5 页，业务/公共分包合计 18 页。
- 另外两个 subpackage 分别为个人资料编辑 1 页、设置 1 页。
- 主包与全部分包合计注册 25 个页面路由。

## 开发与验收注意事项

- 代码和文档只在 `dev` 分支修改；测试环境从 `test` 发布，生产环境从生产分支发布。
- 微信开发者工具导入项目后需要执行“工具 → 构建 npm”，确认 `rxjs` 能从 `miniprogram_npm` 解析。
- 当前尚未取得微信开发者工具服务端口授权，不应为自动化验收擅自开启安全设置。
- 第二、第三阶段仍待开发者工具 npm 构建和包体检查，以及 320px、375px、430px 三档模拟器/真机视觉验收；左滑删除还需真机手势确认。
- Prisma 迁移文件存在不代表数据库已执行迁移；必须先确认专用开发/测试数据库、备份和回滚方案。
- 第四阶段已有本地实现；迁移/清理演练、部署定时任务和故障/性能/脱敏/目标环境代理验收完成前，不得用于发布完成判断。
- 最新完成状态以 `FEATURE_STATUS.md` 为准，体验版操作以 `docs/发布前自测清单.md` 为准。
# 后台发布与冻结规则

- 后台管理员发布普通小区留言、业主互助任务和小区市场商品前，必须绑定一个启用的小程序用户；内容以该用户身份发布，后台仍记录实际管理员操作人。
- 任意启用后台管理员均可直接发布公告；公告不关联或展示为普通小程序用户。
- 被冻结用户不能登录或调用需要登录的接口；公共列表仍可匿名浏览。
- 后台“小区留言”支持按发布用户昵称或用户 ID 查询。
