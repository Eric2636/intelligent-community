# 管理后台说明

Web 管理端与 **`admin` 云函数源码**已放在与小程序工程**同级**的目录：

**`intelligent community admin`**

请在该目录开发与构建管理端；部署云函数时，将该目录下的 `cloudfunctions/admin` **复制到**本小程序项目的 `cloudfunctions/admin` 后，在微信开发者工具中上传部署。

详细步骤见：**`../intelligent community admin/docs/管理后台部署.md`**（从本文件所在仓库根目录向上一级进入该文件夹）。
