<t-tab-bar value="{{ value }}" theme="tag" split="{{ false }}" bind:change="handleChange">
  <t-tab-bar-item wx:for="{{ visibleList }}" wx:key="value" icon="{{ item.icon }}" value="{{ item.value }}" badge-props="{{item.badgeProps}}">{{ item.label }}</t-tab-bar-item>
</t-tab-bar>
